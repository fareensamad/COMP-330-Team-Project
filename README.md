# COMP-330-Team-Project
This project is a music review website

To run:
1. Open this repo in a code editor
2. Run "npm run dev" in the terminal
3. Open the localhost link that appears

if an error occurs saying that vite is not available, run "npm install" and rerun "npm run dev" after
